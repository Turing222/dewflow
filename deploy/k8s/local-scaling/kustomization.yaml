apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../namespace.yaml
  - ../configmap.yaml
  - redis.yaml
  - ../knowledge-files-pvc.yaml
  - ../worker-deployment.yaml
  - worker-keda-local.yaml

patchesStrategicMerge:
  - worker-scaling-demo-patch.yaml
