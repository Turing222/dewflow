apiVersion: v1
kind: Service
metadata:
  name: dewflow-api
  namespace: dewflow
  labels:
    app.kubernetes.io/name: dewflow-api
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dewflow-api
  ports:
    - name: http
      port: 8000
      targetPort: http
